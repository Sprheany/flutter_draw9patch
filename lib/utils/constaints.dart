// ignore_for_file: constant_identifier_names

const String APP_TITLE = "9 Patch Editor";

const double PANEL_MIN_WIDTH = 60;

const double WINDOW_WIDTH = 1024;
const double WINDOW_HEIGHT = 600;

const double MIN_PATCH_SCALE = 2;
const double MAX_PATCH_SCALE = 6;
const double STRETCH_MARGIN = 48;

const String EXTENSION_9PATCH = ".9.png";

const double STRIPES_WIDTH = 4.0;
const double STRIPES_SPACING = 6.0;
const int STRIPES_ANGLE = 45;

/// The fraction of the window size that the 9patch should occupy.
const double IDEAL_IMAGE_FRACTION_OF_WINDOW = 0.7;

/// Default zoom level (in percentage) for the 9patch image.
const double DEFAULT_ZOOM = 100;

/// Minimum zoom level (in percentage) for the 9patch image.
const double MIN_ZOOM = 30;

/// Maximum zoom level (in percentage) for the 9patch image.
const double MAX_ZOOM = 800;
